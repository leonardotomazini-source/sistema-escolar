# render.yaml
services:
  - type: web
    name: sistema-escolar
    env: python
    startCommand: "gunicorn wsgi:app"
    buildCommand: "pip install -r requirements.txt"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11